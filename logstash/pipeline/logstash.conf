input {
	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here
filter {
  mutate {
    lowercase => "appname"
  }
}
output {
        elasticsearch {
                hosts => "elasticsearch:9200"
                index => "emsa-%{+YYYY.MM.dd}"
        }
}
